
global = undefined;

// footer begin
}.call({}, [])); // Запускаем в контексте простого объекта, чтобы не пустить window.

}.call({}, _)); // Запускаем в контексте простого объекта, чтобы не пустить window.